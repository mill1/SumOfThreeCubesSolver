namespace SumOfThreeCubesSolver.Extensions
{
    public static class DoubleExtensions
    {
        public static double Pow(this double toBeRaised, double powerOf)
        {
            return Math.Pow(toBeRaised, powerOf);
        }
    }
}
